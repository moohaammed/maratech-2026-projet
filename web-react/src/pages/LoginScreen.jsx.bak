import { useState } from 'react';

export default function LoginScreen() {
    return (
        <div style={{
            minHeight: '100vh',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            backgroundColor: '#f5f5f5',
            color: '#333'
        }}>
            <h1>Login Screen Test</h1>
            <p>If you see this, the routing works perfectly.</p>
            <p>The previous crash was due to a syntax or runtime error in the complex UI code.</p>
            <button onClick={() => window.location.reload()} style={{ padding: '10px 20px', marginTop: 20 }}>
                Reload Page
            </button>
        </div>
    );
}
